@page "/orchestration"
@inject AgenticSdlc.Web.Services.WindowManagerService WM
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        WM.OpenApp("workflow", "Workflow", "🕸", w: 1080, h: 660);
        Nav.NavigateTo("/", replace: true);
    }
}
